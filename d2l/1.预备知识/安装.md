使用下面的命令创建一个新的环境：
`conda create --name d2l python=3.9 -y`

现在激活 `d2l` 环境：
`conda activate d2l`

安装`pytorch`：
`pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu126`

下载Jupyter记事本：
`https://zh-v2.d2l.ai/d2l-zh.zip`

安装需要的包：
`pip install jupyter d2`

运行`jupyter notebook`
